# MCPloit — Python dependencies
# Install: pip install -r requirements.txt
# Recommended: use a virtualenv
#   python3 -m venv .venv && source .venv/bin/activate

# ── Core MCP client ────────────────────────────────────────────────────────
fastmcp>=2.3.0

# ── CLI framework ──────────────────────────────────────────────────────────
typer>=0.12.0
click>=8.1.0               # typer dependency

# ── Rich terminal output ───────────────────────────────────────────────────
rich>=13.7.0

# ── HTTP client (active probing, Burp proxy, network scanner) ─────────────
httpx>=0.27.0

# ── AI review layer (Layer 2) ──────────────────────────────────────────────
anthropic>=0.34.0          # required for --ai flag; optional otherwise

# ── Async ─────────────────────────────────────────────────────────────────
anyio>=4.4.0

# ── Standard library supplements ──────────────────────────────────────────
# (ast, re, ssl, socket, pathlib, dataclasses, enum — all stdlib, no install needed)
